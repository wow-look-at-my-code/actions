name: Test secret-server
version: 1
description: Run the secret-server dats suite (never released)

runs:
  using: composite
  steps:
    - uses: wow-look-at-my/dats@master
      with:
        tests: secret-server/dats
